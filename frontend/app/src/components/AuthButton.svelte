<div class="gap-x-3">
    <div class="flex-none">
        <button class="btn btn-ghost text-lg" on:click={()=>window.location = '/register'}>Register</button>
    </div>
    <div class="flex-none">
        <button class="btn btn-primary text-lg" on:click={()=>window.location = '/login'}>Sign In</button>
    </div>
</div>
